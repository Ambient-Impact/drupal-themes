// -----------------------------------------------------------------------------
//   Ambient.Impact - Site theme - Fonts
// -----------------------------------------------------------------------------

AmbientImpact.on('font', function(aiFont) {
AmbientImpact.addComponent('siteThemeFont', function(siteThemeFont, $) {
  'use strict';

  // Load fonts.
  aiFont.load({
    furore: 'Furore'
  });
});
});
