{#
/**
 * @file
 * Image formatter link to image style theme implementation to display a
 * formatted image field.
 *
 * This is altered from the one that ships with the module to use the link()
 * function so that a '#type' => 'link' is rendered, allowing hooks to alter it.
 *
 * Additionally, the 'linkAttributes' variable has been added, replacing the
 * 'url_rel', 'url_class', and 'url_attributes' variables.
 *
 * Available variables:
 *
 * - image: A collection of image data.
 *
 * - image_style: An optional image style.
 *
 * - url: An URL the image can be linked to.
 *
 * - linkAttributes: An array of attributes to add to the link if 'url' is set.
 *
 * @see template_preprocess_image_formatter_link_to_image_style_formatter()
 *
 * @see ambientimpact_media_preprocess_image_formatter_link_to_image_style_formatter()
 *
 * @see ambientimpact_media_theme_registry_alter()
 *
 * @ingroup themeable
 */
#}

{{ link(image, url, linkAttributes) }}
